команды cmd:
-	cls	чистит строку cmd
-	cd и пусть к файлу


команды для диплома:
-	pip install pyside	скачиваем библиотеку интерфейса QTdesigner, точнее его реализацию на пайтоне
-	pip install PySide2	
-	pyside-uic "путь к файлу.ui" -x -o "место где сохраним файл.py"		переводим файл .ui ---> .py
-	сначало заходим в папку с файлом	переводим файл .ui ---> .py
	pyside2-uic ФАЙЛ.ui -x -o ФАЙЛ.py
	pyuic5 ФАЙЛ.ui -x -o ФАЙЛ.py
-	pyinstaller -F main.py		перевод файл .py ---> .exe
-	pip install pyqt5-tools		скачиваем что-то(провеерить)

vs:
-	CTRL + SHIFT + P
-	CTRL + /